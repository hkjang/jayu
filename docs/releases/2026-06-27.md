# Release Notes - 2026-06-27

## Added

- Feature inventory generator for Python modules, CLI commands, Dashboard API routes, static dashboard sections, tests, and docs.
- Feature status matrix seed in `configs/feature_status.yaml`.
- Dashboard and CLI coverage map generators.
- Release doctor command for release-readiness checks.
- Release notes generator based on changed files and feature inventory.
- Config wizard command that writes a starter config and `.env.example` template without storing secrets.

## Dashboard

- Added `GET /api/v1/features`.
- Added Feature Inventory & Status Matrix section to the System/Settings page.

## Toss Order History

- Aligned cached order history with Toss Order History `GET /api/v1/orders`.
- Added order detail support for `GET /api/v1/orders/{orderId}` and cache file `state/toss_order_details.json`.

## Verification

- `ruff check` passed for changed Python files.
- `pytest tests/test_smoke_integrated.py tests/test_personal_finance_extensions_phase3.py::TestTossOrdersManager tests/test_toss.py -q` passed.
- `node --check` passed for changed dashboard JavaScript files.

## Note

`jayu.exe release doctor` could not be executed in this workspace because the local venv entrypoint points to a missing uv-managed Python 3.12 runtime.
